platform: linux
image_resource:
  type: docker-image
  source: {repository: amidos/dcind, tag: 2}
inputs:
  - name: image
  - name: repo
run:
  path: bash
  args:
    - -exc
    - |
      cd repo &>/dev/null
      ((script))